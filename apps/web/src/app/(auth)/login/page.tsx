// Login screen. Compose the LoginForm feature component here — no data logic in pages.
// TODO: import { LoginForm } from '@/features/auth/components/login-form'
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-muted-foreground">TODO: login form</div>
    </main>
  )
}
