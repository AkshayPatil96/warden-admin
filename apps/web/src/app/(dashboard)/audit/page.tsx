// Audit log viewer — every sensitive action with actor, timestamp, before/after.
// One of the strongest "real product" signals; surface it prominently.
export default function AuditPage() {
  return <div className="text-muted-foreground">TODO: audit log table</div>
}
